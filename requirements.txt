# Unified VAR requirements — Python 3.12 compatible
#
# Install in two steps to preserve the CUDA build:
#   1. pip install torch==2.5.1 torchvision==0.20.1 torchaudio==2.5.1 --index-url https://download.pytorch.org/whl/cu121
#   2. pip install -r requirements.txt
#
# Activate venv first: C:/Users/neeld/VAR/venv/Scripts/activate

# Core ML / CV
ultralytics>=8.2.0
opencv-python>=4.8.0,<4.11
numpy>=1.26.0,<2
scipy>=1.12.0,<1.14

# Foul Detection
pandas>=2.1.0
matplotlib>=3.8.0
imageio>=2.34.0
einops>=0.7.0
av>=12.0.0

# Offside Detection
scikit-learn>=1.3.0
Pillow>=10.0.0
tqdm>=4.66.0
kornia>=0.7.0

# GUI
PyQt5>=5.15.10
